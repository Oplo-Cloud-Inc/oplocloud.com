# macOS
.DS_Store
**/.DS_Store

# Local agent memory & logs — not part of the site
.remember/
**/.remember/

# Dependencies, builds, logs
node_modules/
**/node_modules/
*.log

# wrangler's local state and cache — a local database can hold student records
.wrangler/
**/.wrangler/

# Secrets — never commit real credentials
.env
**/.env
oplo-accounts/.env
